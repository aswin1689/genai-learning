# Session Log

One line per session: what you built, what surprised you. Read it back on low-motivation
days — it's proof of momentum, and raw material for portfolio write-ups and interview
"hardest part" stories later.

Format: `YYYY-MM-DD — <what you built> — <what surprised you / what broke>`

---

- 2026-07-10 — Built `embeddings_semantic_search.py`: embedded ~10 short IT-runbook
  paragraphs, matched a plain-English query to the right one via cosine similarity with
  zero shared words — surprising how well a 384-number vector captures "meaning."
